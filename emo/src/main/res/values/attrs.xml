<?xml version="1.0" encoding="utf-8"?>
<resources>

    <declare-styleable name="Emo">
        <attr name="emoSize" format="dimension" />
        <attr name="emoTextStart" format="integer" />
        <attr name="emoTextLength" format="integer" />
    </declare-styleable>

    <dimen name="keyboard_height">250dp</dimen>

    <style name="emoDialog" parent="@android:style/Theme.Dialog">
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">fill_parent</item>

        <!-- No backgrounds, titles or window float -->
        <item name="android:windowFrame">@null</item>
        <item name="android:windowBackground">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
    </style>

</resources>